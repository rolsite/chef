{
  "framework": "remix",
  "buildCommand": "CONVEX_VERBOSE=1 pnpx convex deploy --cmd \"pnpm run build\" --cmd-url-env-var-name VITE_CONVEX_URL --preview-run 'sessions:issueInviteCodeForPreviewDeployment'",
  "installCommand": "pnpm install",
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        { "key": "Cross-Origin-Embedder-Policy", "value": "require-corp" },
        { "key": "Cross-Origin-Opener-Policy", "value": "same-origin" }
      ]
    }
  ]
}
